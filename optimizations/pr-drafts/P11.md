# PR title: feat(metrics): REST client latency histograms
Branch: aditya-shantanu:upstream-p11-restclient-metrics

#### What this PR does / why we need it:

The controller currently exposes no visibility into apiserver round-trip latency as seen by the client: controller-runtime's `pkg/metrics` wires only client-go's `RequestResult` hook (`rest_client_requests_total`), so request counts are exported but request latency is not.

This PR adds a small `k8s.io/client-go/tools/metrics` adapter (`internal/metrics/restclient.go`) that registers two histograms into the controller-runtime Prometheus registry, using the same metric names as `k8s.io/component-base` so standard dashboards and queries apply:

- `rest_client_request_duration_seconds{verb, host}` — client-observed latency of Kubernetes API requests. The URL path is deliberately dropped to bound cardinality; the delta against the apiserver's own `apiserver_request_duration_seconds` exposes network+client overhead.
- `rest_client_rate_limiter_duration_seconds{verb, host}` — time requests wait on the client-side rate limiter before being sent. Non-zero values under burst load indicate `--kube-api-qps`/`--kube-api-burst` are the bottleneck rather than the apiserver.

Together these make write RTTs, watch re-establishment, and client-side queueing visible on the `/metrics` endpoint. We used these histograms to attribute latency while diagnosing warm-adoption performance in a 300-claim warm-adoption benchmark.

The hooks are self-registering at package init (the `internal/metrics` package is already imported by the controller binary), there is no behavior change, and the overhead is a single histogram observation per REST request — negligible.

Includes a unit test verifying observations made through the client-go hooks land in the registered histograms with the expected `verb`/`host` labels.

#### Which issue(s) this PR is related to:

Ref #245 (observability umbrella)

#### Release Note

```release-note
The controller now exposes REST client latency metrics: `rest_client_request_duration_seconds` and `rest_client_rate_limiter_duration_seconds` histograms (labeled by `verb` and `host`) on the metrics endpoint.
```

## A/B protocol (deferred — run before filing this PR)

Single kops GCP cluster, reused for BASE and candidate legs; the A/B isolates
the code change, not the cluster. Full learned cluster configuration, applied
IDENTICALLY to both legs:

- Cluster: NODE_COUNT=6 workers, n2-standard-8 (e2-standard-8 if N2 quota is
  tight), NODE_VOLUME_SIZE=100, control plane n2-standard-16.
- TUNE_CONTROL_PLANE=true: apiserver inflight raises; kube-scheduler client
  QPS 800/1600 via the cluster-yaml patch mechanism (kops v1.35 `--set` on
  `kubeScheduler.kubeAPIQPS` is silently inert — the round-10 cluster-spec
  patch mechanism is required); etcd quota-backend-bytes=8GiB + 2m periodic
  auto-compaction on both main and events clusters (etcdClusters[*].manager.env).
- TUNE_NODES=true: kube-apiserver eventTTL=15m, kubelet system reserves,
  100GB pd-ssd worker boot disks.
- CNI cilium with the enablePrometheusMetrics raise (creation-time).
- Clean instrumentation: stock logging, no --enable-pprof-debug, no zap debug
  level, no -v, no in-burst profiling on measurement legs.
- Harness: 300-claim warm-adoption burst (claims-warm phase),
  STRESS_SMOKE_COUNT=20 smoke gate before each full burst,
  --client-connections=4, harness capacity-math validation on; identical
  harness build drives every leg.
- Controller args on BOTH legs: --leader-elect=true --enable-webhook=false
  --sandbox-concurrent-workers=400 --sandbox-claim-concurrent-workers=400
  (plus the leg-specific flags below).
- BASE leg: stock upstream/main image (9a4b3a0) on the identically tuned cluster.
- Teardown: kops delete cluster + verified deletion (poll `kops get cluster`
  until absent; assert zero leftover GCE instances).
- Report per leg: claim create->Ready p50/p90/p99 + time-to-all-ready, from
  the same harness build.

Leg-specific plan for this PR (metrics-only change):
- Candidate leg: this branch's image, no extra flags. Success criteria are
  (a) no measurable burst-latency regression vs BASE (histograms are
  observe-only), and (b) the two histograms present and populated in the
  controller's /metrics scrape during the burst.
